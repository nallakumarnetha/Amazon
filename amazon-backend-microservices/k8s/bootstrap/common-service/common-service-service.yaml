apiVersion: v1
kind: Service
metadata:
  name: common-service
  labels:
    app: common-service
spec:
  selector:
    app: common-service
  ports:
    - port: 80         # exposed inside the cluster
      targetPort: 8082 # containerPort in deployment
  type: ClusterIP
