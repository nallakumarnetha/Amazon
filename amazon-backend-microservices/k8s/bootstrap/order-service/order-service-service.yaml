apiVersion: v1
kind: Service
metadata:
  name: order-service
  labels:
    app: order-service
spec:
  selector:
    app: order-service
  ports:
    - port: 80         # exposed inside the cluster
      targetPort: 8084 # containerPort in deployment
  type: ClusterIP
