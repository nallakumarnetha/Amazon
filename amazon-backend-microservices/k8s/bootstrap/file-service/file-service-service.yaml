apiVersion: v1
kind: Service
metadata:
  name: file-service
  labels:
    app: file-service
spec:
  selector:
    app: file-service
  ports:
    - port: 80         # exposed inside the cluster
      targetPort: 8083 # containerPort in deployment
  type: ClusterIP
