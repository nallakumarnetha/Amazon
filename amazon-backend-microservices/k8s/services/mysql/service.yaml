apiVersion: v1
kind: Service
metadata:
  name: mysql
spec:
  clusterIP: None       # Headless service (required for StatefulSet)
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
